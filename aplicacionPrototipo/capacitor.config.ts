import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prototipo.com',
  appName: 'aplicacion-prototipo',
  webDir: 'dist/aplicacionPrototipo/browser'
};

export default config;
