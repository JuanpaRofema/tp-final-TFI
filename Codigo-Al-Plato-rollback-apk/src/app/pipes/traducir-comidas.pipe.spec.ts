import { TraducirComidasPipe } from './traducir-comidas.pipe';

describe('TraducirComidasPipe', () => {
  it('create an instance', () => {
    const pipe = new TraducirComidasPipe();
    expect(pipe).toBeTruthy();
  });
});
